Make the following changes:
- Change the off-white theme to be more of a beige, it should feel like parchment paper
- In the schema view make the right side bar a seperate floating box so you can see the dot grid background like the other 3 sections
- Shrink each of the floating boxes by 30% so there is a more distinct seperate and you can see more of the dot-grid in the background
- Add gruv-box dark to the themes
- Make the highlight when hovering over a button change depending on the them (for instance darker themes with the white highlight is jarring)
- The Job Title in the schema should be based on where the job title by default is on the scham (for instance the job title on the schema we have is right below the name)
- Fix the pdf export, it still includes a white border all around it
- Move all navigation features to the left sidebar (for instance the back to variations button is on the right side bar at the moment)
- Add a dotted line on the top ribbon between each of the options