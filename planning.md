Changes:
- Change the schema and variation selections to be the rectangular page like appearance. Each should look like the schema/variation you're clicking on.
- the configurations on is specifically for stuff like "open", "edit", etc when you highlight a schema/variation
  - Bring back the config cog in the bottom left
- The main window doesn't change color with the theme
- I want the ui to look more polished. Take some queues from the attached image (aka, the https://filesmith.io/ website). 
  - I want the homepage ui cropped in a bit, with some space between each of the 3 sections (left navigation and config bar, top menu ribbon, and main section)
  - Make the background similar to the spaced out dots (these should be impacted by the themes)
- In the resume maker
  - Make "export as" the title of the drop down, then it lets you select the export type (like a file->export-><filetype> drop-down tree)
  - The pdf export still looks awful. Check Ethan Owens Resume.pdf. It should look exactly like the document in the schema
  - The name auto filling doesn't work. Job title isn't being pulled from the schema like it should
  - The dark mode text on the sidebar is too hard to see (should be a brighter grey)
  - Change  the sidebar in the resume maker menu to be on the right side